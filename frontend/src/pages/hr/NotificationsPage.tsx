import StaffNotificationsView from '@components/notifications/StaffNotificationsView'

/** Trang thông báo đầy đủ cho HR Admin — dùng chung view nhân sự. */
export default function HrNotificationsPage() {
  return <StaffNotificationsView />
}
