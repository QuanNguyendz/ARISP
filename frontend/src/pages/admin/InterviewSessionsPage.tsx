import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';

export default function InterviewSessionsPage() {
  return (
    <Box>
      <Typography variant="h4">Interview Sessions</Typography>
    </Box>
  );
}
