import HelpCenterView from '@/components/help/HelpCenterView'

/** Trợ giúp & tài liệu cho Chuyên viên tuyển dụng — không có mục Playbook (không có quyền upload). */
export default function RecruiterHelpPage() {
  return <HelpCenterView role="recruiter" />
}
