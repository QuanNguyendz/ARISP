import HelpCenterView from '@/components/help/HelpCenterView'

/** Trợ giúp & tài liệu cho HR Lead — bản đầy đủ, có mục Playbook. */
export default function HrHelpPage() {
  return <HelpCenterView role="hr_admin" />
}
